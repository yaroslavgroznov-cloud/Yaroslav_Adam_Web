# Production env для Cloudflare Pages деплоя.
# Sprint E (Single OTP fix, 2026-05-24):
# Frontend на adam.groznov.uk проксирует /adam/* и /admin/* через
# Pages Functions (functions/adam/[[path]].ts) на adam-api.groznov.uk.
# Для same-origin fetch VITE_ADAM_API_BASE остаётся ПУСТЫМ —
# в api/adam.ts URL relative (/adam/...). Один OTP покрывает всё.

VITE_ADAM_API_BASE=
