# Production env для Cloudflare Pages деплоя.
# Vite читает этот файл при `npm run build` (NODE_ENV=production).
# Sprint C deploy follow-up, 2026-05-23.
#
# Frontend на adam.groznov.uk (Pages), backend на adam-api.groznov.uk (Tunnel).
# Оба имени — 1-й уровень от корня, Universal SSL покрывает *.groznov.uk wildcard.
# fetch с credentials:include требует точного origin в CORS.

VITE_ADAM_API_BASE=https://adam-api.groznov.uk
