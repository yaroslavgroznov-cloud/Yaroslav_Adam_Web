# Adam Web — переменные окружения для разработки.
# В production (на Cloudflare Pages) переменные не нужны: фронт и backend на одном
# origin adam.groznov.uk, fetch идёт относительный.

# В dev указываем абсолютный URL до backend Штаба (или туннеля):
VITE_ADAM_API_BASE=http://localhost:8003
